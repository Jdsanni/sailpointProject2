#To build the docker image, we can run the command
docker build -t github-pull-requests .

#To run the docker image, we can run the command
docker run -it github-pull-requests

#After running the last command your docker file will be built and the project us done
#this is also to confirm what i am doing with webhook
